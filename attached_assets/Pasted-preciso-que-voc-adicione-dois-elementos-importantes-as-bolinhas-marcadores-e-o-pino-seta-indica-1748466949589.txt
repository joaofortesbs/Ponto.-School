preciso que você adicione dois elementos importantes: as bolinhas (marcadores) e o pino (seta indicadora). A roleta tem 6 divisões, e quero que tudo seja implementado corretamente, seguindo as especificações abaixo.

Especificações:
1. Adicionar as bolinhas (marcadores):
A roleta tem 6 divisões, ou seja, cada fatia ocupa 60 graus (360° / 6).
As bolinhas devem ser posicionadas nas linhas divisórias entre as fatias, ou seja, nos ângulos: 0°, 60°, 120°, 180°, 240°, 300°.
As bolinhas devem estar na borda da roleta, exatamente sobre as linhas divisórias.
Use a fórmula abaixo para calcular as posições das bolinhas:
javascript

Copiar
const totalSegments = 6; // 6 divisões
const centerX = roletaWidth / 2; // Centro X da roleta
const centerY = roletaHeight / 2; // Centro Y da roleta
const desiredRadius = roletaRadius; // Raio da roleta (ajuste se necessário para a borda)

for (let index = 0; index < totalSegments; index++) {
    const angle = index * (360 / totalSegments); // 0°, 60°, 120°, 180°, 240°, 300°
    const radians = (angle * Math.PI) / 180;
    const x = centerX + desiredRadius * Math.cos(radians);
    const y = centerY + desiredRadius * Math.sin(radians);
    placeMarker(x, y); // Posicione a bolinha nas coordenadas (x, y)
}
Certifique-se de que o desiredRadius seja ajustado para que as bolinhas fiquem na borda da roleta, sobre as linhas divisórias, e não dentro das fatias.
2. Adicionar o pino (seta indicadora):
O pino deve ser posicionado na parte externa da roleta, apontando para dentro, indicando qual fatia foi sorteada.
Coloque o pino no ângulo 0° (ou seja, na parte superior da roleta, alinhado com a borda).
Certifique-se de que o pino esteja apontando para dentro da roleta. Se necessário, aplique uma rotação para corrigir a direção:
css

Copiar
transform: rotate(180deg);
Se estiver usando Canvas, SVG ou outra tecnologia, ajuste a rotação ou o vetor de direção para que o pino aponte para dentro.
Resultado esperado:
A roleta deve ter 6 bolinhas posicionadas nas linhas divisórias (ângulos 0°, 60°, 120°, 180°, 240°, 300°), na borda da roleta.
O pino deve estar na parte superior externa da roleta, apontando para dentro, indicando claramente a fatia selecionada.
Por favor, implemente esses elementos no código da roleta e teste para garantir que tudo esteja funcionando corretamente. Se precisar de mais detalhes ou ajustes, posso fornecer! Obrigado!