Quero que você implemente dentro da minha plataforma uma nova funcionalidade de compartilhar atividades personalizadas via link único público. Siga as instruções cuidadosamente:

🔹 Contexto da Plataforma

Já tenho um sistema que gera atividades personalizadas (exemplo: matemática, português, geografia, etc.) a partir de um template.

O usuário pode visualizar a atividade dentro de um modal (Visualizar Atividade).

No dropdown desse modal já existe a opção Compartilhar (como mostrei nas imagens).

🔹 O que preciso implementar

Quando o usuário clicar em "Compartilhar":

O sistema deve gerar um link único e público para aquela atividade.

Esse link precisa ser acessível mesmo para pessoas sem conta na plataforma.

Estrutura técnica:

Cada atividade terá um id único no banco de dados.

Esse id deve ser usado para gerar uma rota pública, no formato:

https://minha-plataforma.com/atividade/:id


Essa rota não deve usar a interface normal da plataforma.

Criar um arquivo dedicado, por exemplo:

/pages/publicActivity/[id].jsx


Esse arquivo será uma interface própria (separada), para que eu possa editar no futuro.

Interface pública da atividade:

Quando alguém acessar o link, abrir uma página simples, limpa e responsiva.

No centro da tela, deve ter um card estilizado exibindo os dados da atividade.

Esse card deve puxar as informações da atividade (title, description, conteúdo, etc.) a partir do id.

Adicionar um botão "Apresentar para Alunos", que ativa um modo de apresentação (visualização interativa da atividade).

Banco de dados / Backend:

Criar uma rota de API, por exemplo:

/api/publicActivity/:id


que retorna os dados da atividade (sem exigir autenticação).

Garantir que os dados públicos não exponham informações privadas do usuário (apenas o conteúdo da atividade).

Fluxo resumido:

Usuário clica em Compartilhar no modal.

O sistema gera o link único (/atividade/:id).

Esse link pode ser copiado e enviado para qualquer pessoa.

Qualquer pessoa que acessar o link verá a página pública dedicada com o card da atividade.

🔹 Requisitos Extras

Criar a função gerarLinkCompartilhamento(id) que retorna a URL pública da atividade.

Adicionar no dropdown do modal a ação de copiar o link gerado.

A interface pública deve estar em um arquivo separado, fácil de personalizar futuramente.

Estilizar a interface com um layout limpo e moderno (usar Tailwind se já estiver habilitado no projeto).

Adicionar tratamento de erro caso o id não exista (exibir mensagem “Atividade não encontrada”).

🔹 Exemplo de Estrutura de Arquivos
/components
   ActivityCard.jsx   → Card que exibe a atividade
/pages
   /publicActivity/[id].jsx  → Página pública dedicada para exibir a atividade
/api
   /publicActivity/[id].js   → Endpoint que retorna dados da atividade pelo ID
/utils
   generateShareLink.js      → Função que gera o link único da atividade


👉 Com isso, quero que você programe todo o sistema de compartilhamento via link único público, incluindo:

Backend (rota da API).

Frontend (página dedicada para exibir a atividade).

Função para gerar o link.

Integração no dropdown do modal.