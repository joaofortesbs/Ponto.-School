Agora o seu objetivo é me falar de maneira clara como deve ser o arquivo que a API da IA que gera as respostas dentro do chat de School Power vai ter acesso, para realizar dentro do próprio chat os comandos e chamadas API para conseguir executar todas as funcionalidades que eu colocar dentro dela, porque esse arquivo precisa conter as propriedades de ações principais, como por exemplo "Pesquisar" ou "Criar", e dentro de cada um desses arquivos vão ter as chamadas especifica que vão controlar os comandos exatos/direcionados, como por exemplo (Pesquisar/atividades-conta) -> (Cuida da pesquisa de todas as atividades que o usuário criou dentro da sua conta ao vivo), e assim por diante! Essa arquitetura precisa funcionar de maneira que a IA quando for gerar a resposta, consiga executar esses comandos, acionando alguma função especifica dentro desses arquivos (Com a mesma API com o mesmo contexto acompanhando tudo dentro de cada função e tarefa) e conseguindo finalizar essa tarefa, e salvar os dados, do jeito que a gente planejou anteriormente!

Precisamos ter um sistema que a IA antes de começar a escrever as resposta, precisa montar um plano de execução em formato de card que vai aparecer dentro da interface de chat, abaixo da primeira mensagem da IA (Ex: Ótimo user, agora eu vou montar um plano de ação pra você:)! E depois que o usuário clicar no botão de Executar plano, a IA vai começar a seguir esse plano de ação!

Analise e planeje de maneira exata como vai ser toda a arquitetura e orquestração, e melhore esse promtp que eu fiz completando ele de maneira perfeita:

- Quando o usuário está dentro da interface principal dentro da seção School Power, e ele escreve o se primeiro prompt dentro da caixa de enviar mensagens, quando ele enviar esse prompt, a interface vai atualizar para uma nova interface, que vai se chamar (Interface modo de produção de chat) com o arquivo exclusivo de (/interface-modo-produção-chat/), que vai cuidar de toda a interface que vai aparecer depois que o usuário enviou o prompt!

Essa interface vai funcionar da seguinte maneira, a interface vai ser um chat com IA, cujo absoltaumente todas as configurações podem ser modificadas dentro dos arquivos, de maneria super clara e milimetrica! E o chat vai funcionar da seguinte maneria, ao invés de aparecer o Card de Construção, com a interface de Quiz de contextualização, vai existir um arquivo chamado (/agente-jota-chat/) que vai cuidar do funcionamento do chat com a IA/API dentro da interface, e vai ter um arquivo de (/agente-jota-chat/prompt-imput-geral/) que vai cuidar do prompt que a API vai receber para escrever dentro do chat com o usuário! E dentro desse arquivo, vamos ter um outro arquivo dedicado a chamadas de comandos e APIs de execução, chamado (/agente-jota-chat/