// Arquivo simplificado para evitar erros de carregamento
export const preloadAllComponents = () => {
  console.log("Pré-carregamento desativado para simplificar inicialização");
  return Promise.resolve();
};